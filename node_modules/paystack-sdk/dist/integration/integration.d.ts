import { HttpClient } from '../http';
import { BadRequest } from '../interface';
import { FetchTimeoutResponse, UpdateTimeout, UpdateTimeoutResponse } from './interface';
export declare class Integration {
    private http;
    constructor(http: HttpClient);
    fetchTimeout(): Promise<FetchTimeoutResponse | BadRequest>;
    updateTimeout(data: UpdateTimeout): Promise<UpdateTimeoutResponse | BadRequest>;
}
